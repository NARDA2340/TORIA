# Configuración de despliegue para Netlify
# Sitio estático: no hay paso de build, se publica el contenido de public/

[build]
  publish = "public"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
